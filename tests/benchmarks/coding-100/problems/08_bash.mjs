export default [
{ id: "bash-01-top-ip", lang: "bash", prompt: `Bash: 脚本内用 printf 生成一段访问日志(每行格式 "IP 方法 路径 状态码",共 8 行,数据写在脚本里)。统计哪个 IP 出现次数最多,输出该 IP 与其次数,格式严格为 "IP=次数"(如 1.2.3.4=5),无多余空白或换行。只返回 bash 脚本,用 \`\`\`bash 包裹,不要解释。`, test: `[ "$OUT" = "192.168.1.1=4" ]`, canonical: `printf "192.168.1.1 GET /a 200\\n10.0.0.5 POST /b 500\\n192.168.1.1 GET /c 404\\n10.0.0.5 GET /d 200\\n192.168.1.1 POST /e 500\\n172.16.0.9 GET /f 200\\n10.0.0.5 GET /g 200\\n192.168.1.1 GET /h 200\\n" | awk "{c[\\$1]++} END{for(ip in c) print c[ip], ip}" | sort -rn | head -1 | awk "{print \\$2\\"=\\"\\$1}"` },
{ id: "bash-02-group-median", lang: "bash", prompt: `Bash: 脚本内用 printf 生成数据(每行 "组名 数值",共 8 行:a 有 1,2,3;b 有 10,20,30,40;c 有 5)。对每个组计算其数值的中位数(奇数个取正中,偶数个取中间两数的平均),按组名升序输出,每行格式 "组名:中位数"(中位数为整数时不带小数)。预期三行。只返回 bash 脚本,用 \`\`\`bash 包裹,不要解释。`, test: `[ "$OUT" = "$(printf "a:2\\nb:25\\nc:5")" ]`, canonical: `printf "a 3\\na 1\\na 2\\nb 10\\nb 20\\nb 30\\nb 40\\nc 5\\n" | sort -k1,1 -k2,2n | awk "{g[\\$1]=g[\\$1] \\$2 \\",\\"} END{for(k in g){n=split(g[k],v,\\",\\"); n--; if(n%2==1){m=v[(n+1)/2]} else {m=(v[n/2]+v[n/2+1])/2} print k\\":\\"m}}" | sort` },
{ id: "bash-03-semver-max", lang: "bash", prompt: `Bash: 脚本内用 printf 生成 6 行文本,其中混有形如 "vX.Y.Z" 的版本号 token 与噪声;注意有一行包含预发布版本 "v2.0.0-rc"(带后缀,不算正式版本,必须排除)。在所有"作为完整单词出现且无任何后缀"的 vX.Y.Z 中,按主.次.修订做数值比较(非字典序,如 1.10.2 > 1.2.10),输出最高版本号,去掉前导 v,格式 "X.Y.Z"。只返回 bash 脚本,用 \`\`\`bash 包裹,不要解释。`, test: `[ "$OUT" = "1.10.2" ]`, canonical: `printf "release v1.2.10 done\\nbuild v1.10.0 ok\\nv1.2.9 old\\nv2.0.0-rc skip\\nv1.10.2 here\\nnope\\n" | awk "{for(i=1;i<=NF;i++) if(\\$i ~ /^v[0-9]+\\.[0-9]+\\.[0-9]+\\$/) print substr(\\$i,2)}" | sort -t. -k1,1n -k2,2n -k3,3n | tail -1` },
{ id: "bash-04-top-words", lang: "bash", prompt: `Bash: 脚本内用 printf 生成 3 行文本(数据写在脚本里)。统计词频(大小写不敏感,按空白切分单词),输出出现次数最多的前 2 个词;次数相同则按单词字典序升序。两个结果用英文逗号连成一行,每项格式 "词:次数"(如 the:4,cat:3)。只返回 bash 脚本,用 \`\`\`bash 包裹,不要解释。`, test: `[ "$OUT" = "the:4,cat:3" ]`, canonical: `printf "The cat sat on the Mat\\nThe DOG ran the race\\ncat CAT dog\\n" | tr "A-Z" "a-z" | tr -s " \\n" "\\n" | sort | uniq -c | sort -k1,1rn -k2,2 | head -2 | awk "{print \\$2\\":\\"\\$1}" | paste -sd, -` },
{ id: "bash-05-tsv-dept", lang: "bash", prompt: `Bash: 脚本内用 printf 生成 TSV 数据(制表符分隔,每行 "姓名<TAB>部门<TAB>薪资",共 6 行)。按部门聚合:统计人数与最高薪资,按部门名升序输出,每行格式 "部门 count=人数 max=最高薪资"(字段间单个空格)。预期三行。只返回 bash 脚本,用 \`\`\`bash 包裹,不要解释。`, test: `[ "$OUT" = "$(printf "eng count=3 max=150\\nhr count=1 max=80\\nsales count=2 max=90")" ]`, canonical: `printf "alice\\teng\\t100\\nbob\\teng\\t150\\ncarol\\tsales\\t90\\ndan\\tsales\\t90\\neve\\teng\\t120\\nfrank\\thr\\t80\\n" | awk -F"\\t" "{c[\\$2]++; if(\\$3>m[\\$2]) m[\\$2]=\\$3} END{for(d in c) print d, \\"count=\\"c[d], \\"max=\\"m[d]}" | sort` },
{ id: "bash-06-record-maxsum", lang: "bash", prompt: `Bash: 脚本内用 printf 生成由空行分隔的多段记录(数据写在脚本里):每段含若干行 "键 数值"。共 3 段(第1段数值 1,2;第2段 10,5,3;第3段 4,4)。计算每段数值之和,找出和最大的那一段,输出其 1 基序号与和,格式严格为 "idx=序号 sum=和"。只返回 bash 脚本,用 \`\`\`bash 包裹,不要解释。`, test: `[ "$OUT" = "idx=2 sum=18" ]`, canonical: `printf "a 1\\nb 2\\n\\nc 10\\nd 5\\ne 3\\n\\nf 4\\ng 4\\n" | awk "BEGIN{RS=\\"\\"} {s=0; n=split(\\$0, lines, \\"\\n\\"); for(i=1;i<=n;i++){split(lines[i], kv, \\" \\"); s+=kv[2]} if(s>best){best=s; bi=NR}} END{print \\"idx=\\"bi, \\"sum=\\"best}"` },
{ id: "bash-07-pipestatus", lang: "bash", prompt: `Bash: 依次运行 4 个管道命令(数据/命令写在脚本里):(1) false | true (2) true | false (3) ( exit 3 ) | cat (4) grep -q x /dev/null | wc -l(输出重定向到 /dev/null)。对每个管道,取其"管道中第一个命令"的退出码(注意:整个管道的 $? 是最后一个命令的退出码,需用 PIPESTATUS 取首段),将 4 个退出码依次拼接成一个字符串后输出(共 4 个字符,无分隔)。只返回 bash 脚本,用 \`\`\`bash 包裹,不要解释。`, test: `[ "$OUT" = "1031" ]`, canonical: `results=""\nfalse | true; results="\${results}\${PIPESTATUS[0]}"\ntrue | false; results="\${results}\${PIPESTATUS[0]}"\n( exit 3 ) | cat; results="\${results}\${PIPESTATUS[0]}"\ngrep -q x /dev/null | wc -l >/dev/null; results="\${results}\${PIPESTATUS[0]}"\necho "$results"` },
{ id: "bash-08-busy-minute", lang: "bash", prompt: `Bash: 脚本内用 printf 生成 7 行日志,每行以时间戳 "HH:MM:SS" 开头后跟内容(数据写在脚本里)。按 "HH:MM"(分钟)归桶统计条数,找出条数最多的那一分钟,输出 "HH:MM=条数"(如 12:01=4)。只返回 bash 脚本,用 \`\`\`bash 包裹,不要解释。`, test: `[ "$OUT" = "12:01=4" ]`, canonical: `printf "12:01:05 a\\n12:01:30 b\\n12:02:00 c\\n12:01:59 d\\n12:03:10 e\\n12:02:45 f\\n12:01:10 g\\n" | awk "{split(\\$1,t,\\":\\"); k=t[1]\\":\\"t[2]; c[k]++} END{for(k in c) print c[k], k}" | sort -rn | head -1 | awk "{print \\$2\\"=\\"\\$1}"` },
{ id: "bash-09-merge-intervals", lang: "bash", prompt: `Bash: 脚本内用 printf 生成若干区间,每行 "起 止"(整数,共 5 行,数据写在脚本里,顺序未排序)。合并所有重叠或边界相接(start <= 当前已合并区间的 end)的区间,按起点升序输出合并结果,每行格式 "起-止"。预期为合并后的多行。只返回 bash 脚本,用 \`\`\`bash 包裹,不要解释。`, test: `[ "$OUT" = "$(printf "1-6\\n8-12\\n15-18")" ]`, canonical: `printf "1 3\\n2 6\\n8 10\\n15 18\\n9 12\\n" | sort -k1,1n -k2,2n | awk "NR==1{s=\\$1; e=\\$2; next} {if(\\$1<=e){if(\\$2>e) e=\\$2} else {print s\\"-\\"e; s=\\$1; e=\\$2}} END{print s\\"-\\"e}"` },
{ id: "bash-10-quoted-csv", lang: "bash", prompt: `Bash: 脚本内用 printf 生成带表头的 CSV(数据写在脚本里),三列 region,product,amount;其中 product 列被双引号包裹且内部含逗号(如 "widget, deluxe"),因此不能简单按逗号切分。region 始终是行首第一个字段(不含逗号),amount 始终是行尾最后一个字段(不含逗号)。跳过表头,只对 region 为 "west" 的行求 amount 之和并输出该整数。只返回 bash 脚本,用 \`\`\`bash 包裹,不要解释。`, test: `[ "$OUT" = "150" ]`, canonical: `printf "region,product,amount\\nwest,\\"widget, deluxe\\",100\\neast,\\"gadget, mini\\",50\\nwest,\\"simple\\",30\\nwest,\\"thing, a, b\\",20\\neast,\\"x\\",5\\n" | awk "NR>1 {reg=\\$0; sub(/,.*/,\\"\\",reg); amt=\\$0; sub(/.*,/,\\"\\",amt); if(reg==\\"west\\") s+=amt} END{print s}"` }
];
